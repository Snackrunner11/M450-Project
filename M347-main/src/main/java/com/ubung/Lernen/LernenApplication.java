package com.ubung.Lernen;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// SpringBootApplication Annotation für die Klasse LernenApplication
@SpringBootApplication
public class LernenApplication {
	
	// Main Methode für die Klasse LernenApplication
	public static void main(String[] args) {
		SpringApplication.run(LernenApplication.class, args);
	}

}
